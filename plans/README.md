# Improvement plans — index

Generated by an `/improve` audit at commit `9547d4d` (2026-06-12, standard effort). Each plan is self-contained: an executor needs no context beyond the plan file and the repo. Verification gates for every plan: `uv run pytest`, `uv run ruff check $(git ls-files '*.py')`, `uv run ruff format --check $(git ls-files '*.py')`, `uv run mypy` — all must exit 0.

## Recommended execution order

| Order | Plan | Title | Effort | Status |
|---|---|---|---|---|
| 1 | [001](001-redact-fmp-api-key-from-logs.md) | Redact FMP API key from warning logs | S | DONE (`b72918e`) |
| 2 | [002](002-provider-failure-observability.md) | Stop caching failed TradingView scans; log silent fallbacks | M | DONE (`667623d`) |
| 3 | [003](003-revive-correctness-suite.md) | Revive the `test/correctness-verification-suite` branch (~5.8k lines of tests + possible CAGR fix) | M | DONE (`381a8a2` — CAGR off-by-one was real; fix ported, reported CAGR values change) |
| 4 | [004](004-consolidate-ticker-normalization.md) | Consolidate duplicate `tv_to_yf` (fixes latent BSE→.NS bug in insiders) | S-M | DONE (`3e7f306`) |
| 5 | [005](005-staged-mypy-reenable.md) | Staged re-enable of 12 disabled mypy error codes | M (staged) | DONE stages 1-4 (`14b81d1`…`02349a9`) — 6 codes re-enabled, 104 errors fixed; still disabled: `call-overload`(21), `index`(14), `operator`(326), `type-arg`(80), `no-untyped-call`(15), `no-untyped-def`(52) |
| 6 | [006](006-lab-server-origin-check.md) | Reject cross-origin requests to backtest-lab server | S | DONE (`be8373a`) |
| 7 | [008](008-minor-cleanups.md) | Minor cleanups (CLUADE.md deleted as redundant, gitignore output dirs) | S | DONE (`d9f4388`) — step 3 ([india] optional extra) SKIPPED pending user sign-off |
| 8 | [007](007-rolling-module-decomposition.md) | Decompose `run_rolling_backtest` | L | DONE (`1156382`…`cca091c`) — characterization test byte-identical at every step; perf +5.6% (within noise); `run_rolling_backtest` now 64 lines |

Status values: TODO / IN-PROGRESS / DONE / BLOCKED / STALE. Executors: update this column.

## Dependency graph

- 001, 002, 006, 008 — independent, any order.
- 003 → blocks 007 (hard) and is recommended before 004 (soft — regression net).
- 005 — independent; land each stage as its own commit. Avoid running concurrently with 004/007 (annotation churn in the same files).
- 008 step 3 (optional extra) requires explicit user sign-off; skip it otherwise.

## Considered and rejected (do not re-audit without new evidence)

| Claimed finding | Verdict |
|---|---|
| "Committed secrets in `.env`" | **False.** `.env` is gitignored, absent from `git ls-files`, and `git log --all -- .env` is empty. No rotation needed on this evidence. |
| "Look-ahead bias in reserve eligibility, `backtester/core.py:451`" | **False.** `_passes_entry_filters` masks internally to `as_of_ts` via `searchsorted` (core.py:86-89); passing full `bars` leaks nothing. |
| "`regime.py` is dead code" | **False.** Drives the benchmark-regime gate in `rolling.py:85-90`. |
| "Output dirs committed to git" | **False.** Untracked. (Gitignoring them is plan 008 step 2.) |
| "Tests only assert doesn't-crash" | **Overstated.** `test_engine.py` asserts exact fill prices/exit reasons. Real gap = module coverage, addressed via plan 003. |
| "`.env` loader can overwrite env secrets" | **Overstated.** Loader skips keys already in `os.environ` (`backtester/data.py:97`). |
| `except (TypeError, ValueError): pass` in `unusual_volume/output.py:155-159` | **By design** — standard `pd.isna` idiom for list-likes. |
| "Move FMP apikey to a header" | **Not possible** — FMP auth requires the query param. Log redaction (plan 001) is the actionable part. |
| yfinance unbounded `>=0.2.40` pin | **Downgraded.** `uv.lock` pins installs; resilience wrapper guards calls. Revisit only if lockfile discipline changes. |
| `pd.concat` read-modify-write in `cache.py:179` panel appends | **Downgraded.** Lock-protected, ~daily frequency; not worth the rewrite risk now. |

## Deferred (real, but no plan written)

- Consolidating the three batch price-fetch implementations (`earnings_backtest/data.py:747`, `rs_breakout.py:333`, backtester fetchers) — different concurrency/error semantics; revisit after 003+004.
- Direction ideas from the audit, not selected this round: `screener review` (forward-performance tracking of past picks via `history.db`), `screener health` doctor command, unified PIT data layer across the three backtest stacks.

## Not audited at this effort level

earnings_backtest internals, pine DSL evaluation correctness, supply-chain/transitive deps, live API behavior, memory profiling on full universes.
